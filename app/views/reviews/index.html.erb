<div class="reviews-index-container">
  <div class="reviews-title-container">
    <h1 class="text-center"> Star Rating of: <%= @activity.title %></h1>
  </div>

  <div class="review-list-container">
    <% @reviews.each do |review| %>

      <div class="review-card-container">
        <div class="review-profile-pic-container mr-4">
          <%= cl_image_tag(review.user.photo.key) if review.user.photo.attached? %>
        </div>
          <div class="nickname-review-container text-center mt-4">
            <p> # days ago </p>
            <h3><%= review.user.nickname %></h3>
          </div>
            <div class="text-and-stars-container">
              <div class="stars-container">
                <% count = 0 %>
                  <% 5.times do %>
                    <% if review.rating > count %>
                      <i class="fas fa-star"></i>
                    <% else %>
                       <i class="far fa-star"></i>
                    <% end %>
                    <% count += 1 %>
                  <% end %>
              </div>
              <div class="text-review-container align-middle">
                <p>"<%= review.description %>"</p>
              </div>
            </div>
      </div>

    <% end %>
  </div>

</div>

  <div class="btn btn-primary" id="new-review-btn">
    <%= link_to "Add Review", new_activity_review_path(@activity), :class => "btn text-white" %>
  </div>
